import 'package:flutter/material.dart';

class SerachScreen extends StatelessWidget {
  const SerachScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SerachScreen'),
      ),
    );
  }
}
