import 'package:flutter/material.dart';

class ShoppingCar extends StatelessWidget {
  const ShoppingCar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shopping Car'),
    );
  }
}
